const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'models-path': resolver(__dirname, 'src', 'app', 'models'),
  'migrations-path': resolver(__dirname, 'src', 'database', 'migrations'),
  'seeders-path': resolver(__dirname, 'src', 'database', 'seeds'),
}